# 项目管理日报系统 与 一屏总览 —— 需求文档

> 本文档由遍历交付包 `ai_report_intranet.zip`（解压目录 `ai_report_intranet/`）后整理而成，以三大痛点为主线，仅保留与痛点相关的功能需求。
> 文档内容基于仓库内 `README.md`、`快速上手.md`、`docs/`（FR_SPECIFICATION、DEPLOYMENT_INTRANET、USER_MANUAL、TEST_PLAN 等）及 `src/` 源码综合提炼。
> 数据源说明：系统采集源仅两个——**本地 Excel 大盘表** 与 **Jira**。

---

## 1. 项目背景与痛点

PM 在日常 NPI 项目管理中面临的典型问题：

| 痛点 | 说明 |
|------|------|
| **数据分散** | 缺陷/DI/版本/进度等数据分散在多处，PM 需同时关注多项目，缺少统一视图，每天手工聚合 |
| **没有显著大盘表** | 缺乏一张能直观呈现多项目进展与健康度的一屏大盘表，关键状态无法一眼看清 |
| **日报重复劳动** | 每天约 1 小时整理格式、凑邮件，产出效率低 |

本系统即围绕上述三个痛点进行建设，提供对应的解决能力：

- **解决数据分散**：自动采集并聚合本地 Excel 大盘表 + Jira 两大数据源，统一视图。
- **解决没有显著大盘表**：提供一屏总览（健康度、进展、风险），关键状态一目了然。
- **解决日报重复劳动**：规则引擎 + 模板自动生成日报并一键邮件推送，PM 从每天手工整理中解放出来。

---

## 2. 总体架构

```
┌─────────────────────────────────────────────────────────────────┐
│                         展现层：一屏总览（原生 JS）                 │
│   软件总览（健康度）· 日报中心 · 一键邮件推送                     │
├─────────────────────────────────────────────────────────────────┤
│                          接口层：FastAPI                            │
│   报告 API · 采集 API · 项目配置 API                               │
├─────────────────────────────────────────────────────────────────┤
│                     业务分析层                                    │
│   多维度状态聚合 · 健康度计算 · 风险识别                           │
│   规则引擎 + 模板渲染生成日报（进展/缺陷/风险/依赖/节点/门禁）     │
├─────────────────────────────────────────────────────────────────┤
│                     数据接入层（两个真实采集源）                    │
│   ① 本地 Excel 大盘表（版本/PID/晨会/里程碑 等 16+ sheet 导入）      │
│   ② Jira（缺陷 / DI / 版本 Gate 实时 Bug 数据）                     │
├─────────────────────────────────────────────────────────────────┤
│                     发送层（email_mcp 通道）                       │
│                  Outlook MCP（email_mcp）· MCP-SSE                │
└─────────────────────────────────────────────────────────────────┘
```

**技术选型**：

| 类别 | 选型 |
|------|------|
| 后端框架 | FastAPI + SQLAlchemy（SQLite） |
| 前端 | 原生 JavaScript 单页应用（零第三方依赖） |
| 数据源 | 本地 Excel 大盘表（openpyxl 导入）+ Jira（`requests` 对接 REST API） |
| 日报生成 | 规则引擎 + Jinja2 模板渲染（不依赖外部 AI 服务） |
| 发送通道 | Outlook MCP（email_mcp，MCP-SSE 协议） |
| 测试 | pytest |

---

## 3. 功能需求

### 3.1 数据采集与聚合（解决「数据分散」）

| 需求编号 | 需求描述 | 优先级 |
|----------|----------|--------|
| FR-DC-01 | **真实采集源（仅两个）**：① 本地 Excel 大盘表导入（核心数据）；② Jira 采集缺陷/DI/版本 Gate 实时数据 | 高 |
| FR-DC-02 | **大盘表 Excel 导入（核心）**：读取本地 xlsx，支持 12+ 张工作表批量导入 SQLite（示例：442 行大盘表、374 行版本记录、415 行晨会纪要、172 个未释放 PID） | 高 |
| FR-DC-03 | **Jira 采集**：实时拉取缺陷/DI/版本数据，供一屏总览与日报生成使用 | 高 |
| FR-DC-04 | **状态摘要统计**：采集后生成 status_summary，各状态计数加总等于 issues_total | 中 |
| FR-DC-05 | **阻塞识别**：能正确识别 Blocked 状态的 Issue（is_blocked=True） | 高 |

### 3.2 项目配置（管理数据源与收件人）

| 需求编号 | 需求描述 | 优先级 |
|----------|----------|--------|
| FR-PM-01 | **项目档案**：配置项目 JQL、收件人、日报开关 | 高 |
| FR-PM-02 | **收件人页内配置**：在页面直接配置收件人，可持久化保存 | 中 |

### 3.3 一屏总览（解决「没有显著大盘表」）

| 需求编号 | 需求描述 | 优先级 |
|----------|----------|--------|
| FR-OV-01 | **一屏总览**：一屏展示项目关键状态（版本类型分布、健康度、高风险项），关键状态一眼看清 | 高 |
| FR-OV-02 | **数据一眼可读**：总览数据来自大盘表 + Jira 自动聚合，无需手工整理 | 高 |

### 3.4 日报自动生成（解决「日报重复劳动」）

| 需求编号 | 需求描述 | 优先级 |
|----------|----------|--------|
| FR-RP-01 | **规则引擎生成**：基于采集到的 Jira/大盘表数据，通过规则引擎自动拼装七段式日报，不依赖外部 AI 服务 | 高 |
| FR-RP-02 | **模板渲染**：支持 Jinja2 模板渲染路径（`render_with_template`），将采集数据（完成/进行中/阻塞/缺陷/完成率）填入模板生成日报 | 高 |
| FR-RP-03 | **页面自定义输入**：用户在日报编辑页面富文本自定义输入日报内容，可保存/加载草稿，无需配置 API 或模板文件 | 高 |
| FR-RP-05 | **日报七段式**：①头部元信息 ②今日核心进展 ③缺陷与质量状态 ④风险与问题 ⑤外部依赖与跨团队协同 ⑥项目节点变更记录 ⑦NPI 硬开门禁，支持一键生成、预览、发送、历史查看 | 高 |
| FR-RP-06 | **空数据处理**：传入空任务列表时正常生成有效报告结构，不崩溃 | 中 |

---

## 4. 接口需求（FastAPI）

| 接口分组 | 接口 | 说明 |
|----------|------|------|
| 报告 API | `POST /api/reports/generate` | 生成日报 |
| | `GET /api/reports` | 报告列表（分页/时间筛选） |
| | `POST /api/reports/{id}/send` | 发送日报 |
| Dashboard API | `POST /api/dashboard/import` | 大盘表导入 |
| 项目配置 API | `GET /api/projects/{key}` | 查看项目配置 |
| 采集 API | `GET /api/collect/preview` | 采集预览 |

---

## 5. 非功能需求

| 类别 | 需求 | 说明 |
|------|------|------|
| **可维护性** | 文档完备 | 需求/部署/测试/用户手册齐全 |
| **工程质量** | 自动化测试 | 全量 pytest 用例通过 |

---

## 6. 部署与运维

### 6.1 部署要点

- **内网部署前提**：`jira.tcl.com` 有来源 IP 安全策略，仅允许 TCL 内网访问，部署机 IP 必须在 Jira API 白名单内，否则返回 403 HTML。
- **部署前验证**：测试 `GET /rest/api/2/myself`，HTTP 200=可部署，403=IP 不在白名单，401=令牌问题。
- **初始化**：`python run.py --init-db`（初始化数据库 + 示例数据）。

### 6.2 关键配置项（.env）

```env
JIRA_URL / JIRA_USERNAME / JIRA_API_TOKEN / JIRA_PROJECT_KEY   # Jira 必填
SENDER_CHANNEL=email_mcp                                         # 发送通道：email_mcp（Outlook MCP）
EMAIL_MCP_SSE_URL=...                                            # MCP 网关地址（SSE 协议，email_mcp 通道）
```

---

## 7. 测试需求

### 7.1 测试范围

| 模块 | 测试类型 | 覆盖要点 |
|------|----------|----------|
| collectors | 单元+集成 | Jira 采集、数据正确性 |
| generator | 单元+集成 | 规则引擎生成、模板渲染、数据正确性 |
| sender | 单元 | email_mcp（Outlook MCP）发送、重试机制 |
| storage | 单元+集成 | CRUD、查询分页、文件输出 |
| api | 集成 | 接口协议、异常处理、状态码 |
| e2e | 端到端 | 采集→生成→存储→发送 全链路 |

### 7.2 关键测试用例（摘要）

- **采集**：TC-C01 采集正确数据量、TC-C02 阻塞识别、TC-C03 状态摘要统计、TC-C05 空数据不报错
- **生成**：TC-G01 完整结构、TC-G02 规则生成正确性、TC-G03 模板渲染、TC-G06 Markdown→HTML
- **发送**：TC-S01/S02 单/多收件人、TC-S03 空列表
- **存储**：TC-ST01~07 CRUD、筛选、分页、日志
- **API**：TC-API01~07 生成、查询、404、配置、触发、采集预览
- **端到端**：TC-E2E01 全链路、TC-E2E02 模板渲染、TC-E2E03 规则引擎生成

### 7.3 异常场景

| 场景 | 处理方式 |
|------|----------|
| Outlook MCP 发送失败 | 模拟发送并记录日志 |
| 数据库写入失败 | 日志记录 + 不中断流程 |
| 传入空数据 | 正常生成空报告 |

---

## 8. 验收标准

| 编号 | 验收项 | 判定标准 |
|------|--------|----------|
| AC-01 | 一屏总览 | 一屏展示项目关键状态，数据来自大盘表 + Jira 自动聚合 |
| AC-02 | 大盘表导入 | 本地 Excel 大盘表可批量导入并在总览中呈现 |
| AC-03 | 项目配置 | 项目配置（JQL/收件人/日报开关）可保存并生效 |
| AC-04 | 日报发送 | 日报可一键发送（本地 Agent 接 Outlook MCP） |
| AC-05 | 日报生成 | 日报可生成、预览、发送、归档、检索 |
| AC-06 | 自动化测试 | `pytest tests/` 全部通过 |
| AC-07 | 内网部署 | 在白名单机器上可对接真实 Jira 采集数据 |

---

## 9. 项目目录结构（交付包）

```
ai_report_intranet/
├── run.py                     # 启动入口
├── start_dashboard_background.py  # 后台启动服务
├── run_dashboard_server.bat   # 带窗口启动
├── 启动NPI驾驶舱.vbs          # 无窗口后台启动
├── requirements.txt / pytest.ini / .env.example
├── mt9653_release_agent.html  # 总览前端页
├── plan_warning_dashboard.html
├── README.md / 快速上手.md
├── src/                       # 后端源码
│   ├── api.py / api_*.py      # FastAPI 路由
│   ├── dashboard_importer.py  # 大盘表导入 + 16 个 NPI 分析函数（核心引擎）
│   ├── collectors.py / jira_dashboard.py / jira_issue_tracker.py
│   ├── generator.py           # 规则引擎 + 模板渲染生成日报
│   ├── storage.py / models.py / config.py
│   ├── html_report.py         # 邮件 HTML 样式
│   └── senders/               # 发送适配器（email_mcp（Outlook MCP））
├── static/                    # 前端资源（css/js/html）
├── docs/                      # 需求/部署/用户手册/测试
└── tests/                     # 自动化测试
```

---

## 10. 需求优先级总结

| 优先级 | 需求范围 |
|--------|----------|
| **P0（核心必须）** | 本地大盘表 Excel 导入 + Jira 采集、项目配置、一屏总览、日报规则引擎生成、日报一键发送、报告存储归档、FastAPI 接口 |
| **P1（重要）** | 页面自定义输入日报、收件人页内配置 |
