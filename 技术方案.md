# 智能化研发日报生成系统 —— 技术方案

> 文档版本：v1.0
> 关联文档：`需求文档.md`
> 适用范围：`ai_report_intranet/` 交付包（FastAPI + SQLAlchemy + SQLite 的 Python 3.10+ 服务）

---

## 1. 文档说明

本技术方案基于 `需求文档.md` 与实际代码包 `ai_report_intranet/` 逐一核对后编写。**只描述代码中真实存在的能力，不新增任何需求或功能。**

### 1.1 技术方案与需求文档的映射

| 需求文档章节 | 本方案对应章节 |
| --- | --- |
| 产品概述 / 目标用户 | §2 系统定位与总体架构 |
| 功能需求（采集 / 生成 / 发送 / 存储 / 调度 / API） | §3 模块设计 |
| 数据模型 | §4 数据模型 |
| 接口清单 | §5 接口清单 |
| 配置要求 | §6 配置与部署 |
| 性能 / 安全 / 运维 | §7 非功能性说明 |

### 1.2 与代码包的一致性说明

- Jira 对接采用 `requests` 库（HTTP 直连），非 `atlassian-python-api`。
- 发送通道为 **email_mcp（MCP-SSE）** 单一通道（无 SMTP 直发、无 Webhook / Slack）。
- 报告发送接口为 `POST /api/reports/{report_id}/send`。
- **不包含**“容错回退 / 降级快照 / 熔断缓存”等容错机制；数据源失败时仅以 `mock` 或规则引擎保证基础可用（见 §3.4 回退说明）。

---

## 2. 系统定位与总体架构

### 2.1 系统定位

面向研发项目管理场景的**智能化日报生成与自动分发服务**。服务自动从 Jira / Confluence / Excel 大盘采集数据，经 LLM（或规则引擎）生成结构化日报，经审阅评分后渲染为 HTML 邮件，并通过 MCP-SSE（email_mcp）通道自动发送。

### 2.2 总体架构

```
┌────────────────────────────────────────────────────────────────────┐
│                          外部数据源                                 │
│    Excel 大盘表(openpyxl)   Jira REST API(requests)   Confluence    │
└──────────────┬──────────────────────┬──────────────────────┬───────┘
               │                      │                      │
               ▼                      ▼                      ▼
┌────────────────────────────────────────────────────────────────────┐
│                        采集层（DataCollector）                        │
│     DashboardImporter    JiraCollector    ConfluenceCollector        │
└────────────────────────────────┬─────────────────────────────────────┘
                                 ▼
┌────────────────────────────────────────────────────────────────────┐
│                      报告生成引擎（ReportGenerator）                  │
│   skill_bridge 优先 → Prompt 组装 → LLMClient → 审阅评分 → 渲染 HTML │
│                └── 规则引擎回退（render_with_template / 规则生成）   │
└────────────────────────────────┬─────────────────────────────────────┘
                                 ▼
┌────────────────────────────────────────────────────────────────────┐
│                   存储层（SQLAlchemy + SQLite）                       │
│   Report / ReportTemplate / SendLog / ProjectConfig /               │
│   DashboardImport                                                   │
└────────────────────────────────┬─────────────────────────────────────┘
                                 ▼
┌────────────────────────────────────────────────────────────────────┐
│                    发送层（SenderFactory + Adapter）                 │
│              EmailMcpAdapter（MCP-SSE 通道）                         │
└─────────────────────────────────────────────────────────────────────┘
        ▲                                          ▲
        │                                          │
┌───────┴───────────────┐        ┌─────────────────┴────────────────┐
│  调度层（APScheduler） │        │  API 层（FastAPI）                │
│   日报 / 周报          │        │  报告 / 采集 / 集成 / 大盘 /       │
│                       │        │  模板 / 项目 / 调度                │
└───────────────────────┘        └──────────────────────────────────┘
```

### 2.3 技术选型

| 类别 | 选型 | 说明 |
| --- | --- | --- |
| 运行时 | Python 3.10+ | 交付包要求 |
| Web 框架 | FastAPI + Uvicorn | REST API 层 |
| ORM | SQLAlchemy | 数据模型与持久化 |
| 数据库 | SQLite | 单机内网部署，无需独立 DB 服务 |
| 数据采集 | openpyxl / requests | Excel 导入；Jira / Confluence 走 HTTP 直连 |
| LLM | openai SDK（Deepseek / OpenAI 兼容协议） | 由 `LLM_PROVIDER` 切换 |
| 模板 | Jinja2 | 规则模式渲染 |
| 调度 | APScheduler | 定时任务 |
| 发送 | MCP-SSE 会话 | email_mcp 通道，调用 `send_email_notification` tool |
| 校验 | Pydantic Settings | 配置加载 |

### 2.4 架构图

#### 2.4.1 组件架构

```
┌──────────────────────────────────────────────────────────────────────────────┐
│                              智能化研发日报生成系统                              │
├──────────────────────────────────────────────────────────────────────────────┤
│                                                                            │
│  ┌───────────────┐      ┌───────────────────┐      ┌───────────────────┐  │
│  │  前端展示层     │      │    API 层         │      │    调度层          │  │
│  │  一屏总览 /    │─────▶│  FastAPI REST     │      │  APScheduler      │  │
│  │  dashboard    │      │  报告/采集/集成/    │      │  日报 / 周报       │  │
│  │  展示页面      │      │  大盘/模板/项目/调度 │      │                  │  │
│  └───────────────┘      └─────────┬─────────┘      └─────────┬────────┘  │
│                                   │                          │           │
│                                   ▼                          ▼           │
│  ┌──────────────────────────────────────────────────────────────────────┐ │
│  │                         业务核心层                                      │ │
│  │   ┌──────────────┐   ┌──────────────────┐   ┌─────────────────────┐  │ │
│  │   │  采集层       │──▶│  报告生成引擎      │──▶│  存储层              │  │ │
│  │   │ DataCollector│   │  ReportGenerator  │   │  SQLAlchemy + SQLite │  │ │
│  │   │  Jira/Conflu │   │  LLM / 规则回退    │   │  Report/SendLog/...  │  │ │
│  │   │  ence/大盘    │   │  审阅评分 / 渲染    │   │                     │  │ │
│  │   └──────────────┘   └──────────────────┘   └───────────┬─────────┘  │ │
│  │                                                        │             │ │
│  │   ┌────────────────────────────────────────────────────────────────┐  │ │
│  │   │  发送层                                                          │  │ │
│  │   │  SenderFactory → EmailMcpAdapter（email_mcp / MCP-SSE）        │◀─┼──┘ │
│  │   └────────────────────────────────────────────────────────────────┘  │ │
│  └──────────────────────────────────────────────────────────────────────┘ │
│                                      │                                    │
│                                      ▼                                    │
│  ┌───────────────┐      ┌───────────────────┐      ┌───────────────────┐  │
│  │  数据源        │      │    LLM 服务        │      │   邮件发送          │  │
│  │  Excel 大盘表  │      │  Deepseek /       │      │  Outlook MCP      │  │
│  │  Jira REST API│      │  OpenAI（兼容协议） │      │  MCP-SSE          │  │
│  │  Confluence   │      │                   │      │  email_mcp 通道    │  │
│  └───────────────┘      └───────────────────┘      └───────────────────┘  │
│                                                                            │
└──────────────────────────────────────────────────────────────────────────────┘
```

#### 2.4.2 数据流

```
① 采集：DataCollector ──▶ Jira/Confluence/Excel 大盘（openpyxl / requests）
② 生成：ReportGenerator ──▶ skill_bridge → Prompt → LLMClient（或规则回退）→ 审阅评分
③ 渲染：后处理 → html_report.render_full_html（HTML 邮件 + KPI 图表）
④ 存储：Report / SendLog / DashboardImport 写入 SQLite
⑤ 发送：SenderFactory → EmailMcpAdapter → Outlook MCP（email_mcp / MCP-SSE）
⑥ 调度：APScheduler 按日报 / 周报定时触发 ①→⑤
```

> 说明：§2.2 为分层总览，§2.4 展示组件间调用关系与数据流转；两者描述同一系统，不新增功能。

---

## 3. 模块设计

### 3.1 采集层（`collectors.py` / `dashboard_importer.py`）

**职责**：汇总并统一外部数据源数据，输出为标准化 `CollectedData`。

- **`DataCollector`**：采集编排入口，聚合 Jira 任务 + Confluence 文档 + 大盘数据。
- **`JiraCollector`**：通过 `requests` 调用 Jira REST API 查询任务（支持 bearer / basic 认证），解析任务类型、状态、阻塞标记、缺陷、DI 值等。认证信息与弹性策略见 §6。
- **`ConfluenceCollector`**：通过 `requests` 查询 Confluence 页面 / 空间（bearer / basic），附带 `LegacySSLAdapter` 兼容内网自签名证书场景。
- **`DashboardImporter`**：`openpyxl` 解析 Excel 大盘表，写入 `DashboardImport` 表，并提供大盘汇总、发布管理、发布计划等分析。

> 说明：数据源在不可用（如凭证缺失、网络异常）时，`JiraCollector` 等会回退到 `mock` 数据，保证链路可运行；这属于**数据源回退**，而非“容错回退”机制。

### 3.2 报告生成引擎（`generator.py` / `skill_bridge.py` / `html_report.py` / `templates.py`）

**职责**：将采集数据转化为结构化日报 / 周报，并渲染为可发送的 HTML 邮件。

生成流程（`ReportGenerator.generate`）：

```
skill_bridge 优先 → 构建 Prompt → LLMClient 生成 → 审阅评分 → 后处理 → HTML 渲染
```

1. **skill_bridge 优先**：若配置了生成 skill，优先使用 skill 输出，否则走标准 LLM 流程。
2. **Prompt 组装**：`_build_prompt` 将采集数据组织为结构化文本（头部元信息 / 核心进展 / 缺陷质量 / 风险问题 / 外部依赖 / 节点变更）。
3. **LLM 生成**：`LLMClient` 按 `LLM_PROVIDER`（deepseek / openai / mock）调用，`temperature` / `max_tokens` 可配置。
4. **审阅评分**：调用审阅 Agent 对初稿评分 / 纠偏，结果写入报告。
5. **后处理 + 渲染**：`_post_process` 处理后交由 `html_report.render_full_html` 渲染（含 KPI 大盘图表）。

**规则引擎回退**（无 LLM 或调用失败时）：

- `_rule_based_response`：纯规则生成日报，输出头部元信息 / 核心进展 / 缺陷质量 / 风险问题 / 外部依赖 / 节点变更六段。
- `render_with_template`：`Jinja2` 模板渲染，对已完成 / 进行中 / 阻塞 / 缺陷做分类过滤，计算 `completion_rate`（完成率）。

> 说明：需求文档要求的“七段式”日报与代码现状存在差异，详见 §7.1 已知差异说明。

### 3.3 发送层（`senders/`）

**职责**：将生成的报告通过 email_mcp 通道发出。

- **`SenderAdapter`（抽象基类）**：定义统一发送接口。
- **`SenderFactory`**：创建发送适配器。
- **`EmailMcpAdapter`（MCP-SSE）**：管理 MCP SSE 会话，调用 `send_email_notification` tool 发送。

发送通道固定为 **email_mcp（MCP-SSE）**，配置项 `SENDER_CHANNEL` 见 §6。

### 3.4 调度层（`scheduler.py`）

**职责**：按计划自动执行采集 → 生成 → 存储 → 发送流水线，基于 APScheduler。

定时任务：

| 任务 | 说明 |
| --- | --- |
| 日报（daily） | 每日生成并（可选）发送项目日报 |
| 周报（weekly） | 每周生成周报 |

核心方法 `_run_report`：`采集 → 生成 → 存储(Report) → 发送(SendLog)`。调度状态 / 触发见 API 层 `GET/POST /api/scheduler/*`。

### 3.5 API 层（`api.py` / `api_*`）

FastAPI 服务，承载全部 REST 接口，覆盖：报告生成 / 查看 / 发送 / 删除，采集预览，Jira / Confluence / 文档集成，大盘导入，模板管理，项目配置，调度触发。完整接口清单见 §5。

---

## 4. 数据模型（`models.py`，SQLAlchemy + SQLite）

| 表 / 模型 | 用途 | 关键字段 |
| --- | --- | --- |
| `Report` | 报告主体 | id、项目、类型、周期、正文、LLM/规则来源、审阅评分、状态 |
| `ReportTemplate` | 报告模板 | 模板名、Jinja2 内容、类型 |
| `SendLog` | 发送日志 | 报告 id、通道、目标、发送时间、状态、错误 |
| `ProjectConfig` | 项目配置 | 项目 key、通知配置、AGENT_AUTO_SEND 等 |
| `DashboardImport` | 大盘导入 | Excel 原始行 / 解析后结构化数据 |

数据库通过 `init_db` 初始化，并对既有表做**兼容补列**（避免旧库升级报错）。

---

## 5. 接口清单（`api.py`）

### 5.1 报告类

| 方法 | 路径 | 说明 |
| --- | --- | --- |
| POST | `/api/reports/generate` | 生成报告 |
| POST | `/api/reports/metric` | 生成指标报告 |
| GET | `/api/reports` | 报告列表 |
| GET | `/api/reports/{report_id}` | 报告详情 |
| GET | `/api/reports/{report_id}/view` | HTML 预览页 |
| DELETE | `/api/reports/{report_id}` | 删除报告 |
| POST | `/api/reports/{report_id}/send` | **发送报告**（按配置通道） |
| POST | `/api/reports/delivery-daily` | 交付日报生成 |

### 5.2 采集与集成

| 方法 | 路径 | 说明 |
| --- | --- | --- |
| GET | `/api/collect/preview` | 采集数据预览 |
| GET | `/api/jira/bugs` | Jira 缺陷列表 |
| POST | `/api/integrations/jira/search` | Jira 搜索 |
| GET | `/api/integrations/confluence/status` | Confluence 连接状态 |
| POST | `/api/integrations/confluence/search` | Confluence 搜索 |
| POST | `/api/integrations/confluence/pages` | Confluence 页面操作 |
| GET | `/api/integrations/docs/status` | 文档集成状态 |
| POST | `/api/integrations/docs/search` / `fetch` | 文档搜索 / 抓取 |

### 5.3 大盘

| 方法 | 路径 | 说明 |
| --- | --- | --- |
| POST | `/api/dashboard/import` | 导入 Excel 大盘 |
| GET | `/api/dashboard/summary` | 大盘汇总 |
| GET | `/api/dashboard/release-management` / `release-plan` | 发布管理 / 计划 |
| GET | `/api/dashboard/multi-project-daily` | 多项目日报 |

### 5.4 模板 / 项目 / 调度

| 方法 | 路径 | 说明 |
| --- | --- | --- |
| GET/POST | `/api/templates` | 模板列表 / 创建 |
| GET/PUT | `/api/projects` / `{project_key}` | 项目配置 |
| GET | `/api/scheduler/status` | 调度状态 |
| POST | `/api/scheduler/trigger/{report_type}` | 手动触发任务 |

> 附注：`/`、`/dashboard`、`/plan-warning-dashboard` 为展示入口（`include_in_schema=False`）。

---

## 6. 配置与部署

### 6.1 核心配置项（`config.py`）

| 配置分组 | 关键项 | 说明 |
| --- | --- | --- |
| 服务 | `SENDER_CHANNEL` | 发送通道：`email_mcp` |
| MCP | `EMAIL_MCP_SSE_URL` | 邮件 MCP-SSE 服务地址 |
| Jira | `JIRA_*` | 基础 URL、账号 / 令牌、项目 key、查询配置 |
| Confluence | `CONFLUENCE_*` | 基础 URL、账号 / 令牌、空间 |
| LLM | `LLM_PROVIDER` / `DEEPSEEK_*` / `OPENAI_*` / `LLM_TEMPERATURE` / `LLM_MAX_TOKENS` | 生成模型配置 |
| Agent | `AGENT_AUTO_SEND` | 巡检结果是否自动发送（控制草稿 / 自动发送） |

### 6.2 运行要求

- Python 3.10+，依赖见 `requirements.txt`。
- 数据库为 SQLite 文件库，首次运行由 `init_db` 建表 / 补列。
- LLM 未配置密钥或 `LLM_PROVIDER=mock` 时，走规则引擎回退，可离线运行（但数据源 Jira 仍需可用或回退 mock）。

---

## 7. 非功能性说明

- **性能**：单机内网部署；SQLite 轻量存储；采集与 LLM 调用为 I/O 密集，依赖异步（FastAPI + MCP-SSE 会话）。
- **安全**：Jira / Confluence 支持 bearer / basic 认证；内网 Confluence 证书兼容（`LegacySSLAdapter`）；发送走 MCP-SSE 会话。密钥经 Pydantic Settings 环境变量注入。
- **可运维**：调度状态 / 手动触发接口；`SendLog` 记录发送链路；报告来源（LLM / 规则）与审阅评分可追踪。

### 7.1 已知差异说明（文档 vs 代码）

| 项 | 需求文档 | 代码现状 |
| --- | --- | --- |
| 日报章节 | 七段式 | LLM 主路径 Prompt 为五章，规则引擎路径为六段 |
| Jira 对接 | requests | requests（已对齐） |
| 发送通道 | email_mcp | email_mcp（已对齐） |

> 本方案已按需求移除 NPI 分析相关描述；代码中的 NPI 实现（分析函数、门禁、相关接口）仍保留，如需同步删除需另行确认。

---

## 8. 交付物清单

| 交付物 | 说明 |
| --- | --- |
| `src/` 各模块 | 采集 / 生成 / 存储 / 发送 / 调度 / API 实现 |
| `requirements.txt` | 依赖声明 |
| `需求文档.md` | 需求规格（已与代码核对） |
| `技术方案.md` | 本文件 |
