# AI 日报交付包 · 测试用例

> 依据《需求文档.md》功能需求与《技术方案.md》模块设计生成。
> 用例编号规范：`TC-<模块>-<序号>`；每个用例包含：**前置条件、步骤、期望效果**。
> 验收脚本：`ai_report_intranet/tests/test_acceptance_report.py`，用例编号与本文档一一对应。

---

## 一、用例汇总表

| 编号 | 用例名称 | 模块 | 对应需求 | 对应技术方案 |
|------|----------|------|----------|--------------|
| TC-SYS-001 | 系统健康检查 | 系统 | 接口需求 | §3.5 API 层 |
| TC-DC-001 | Jira 数据采集（mock） | 采集层 | FR-DC-01 | §3.1 采集层 |
| TC-DC-002 | 状态摘要统计正确性 | 采集层 | FR-DC-03 | §3.1 采集层 |
| TC-DC-003 | 阻塞任务识别 | 采集层 | FR-DC-04 | §3.1 采集层 |
| TC-DC-004 | 空数据采集不报错 | 采集层 | FR-DC-05 | §3.1 采集层 |
| TC-DC-005 | 大盘摘要接口（mock 回退） | 采集层 | FR-DC-06 | §3.1 采集层 |
| TC-GEN-001 | 日报生成（mock LLM） | 生成引擎 | FR-RP-01 | §3.2 生成引擎 |
| TC-GEN-002 | 周报生成（mock LLM） | 生成引擎 | FR-RP-02 | §3.2 生成引擎 |
| TC-GEN-003 | 模板渲染含关键章节 | 生成引擎 | FR-RP-03 | §3.2.2 模板渲染 |
| TC-GEN-004 | HTML 邮件渲染 | 生成引擎 | FR-RP-04 | §3.2.3 HTML 渲染 |
| TC-GEN-005 | 审阅评分落库 | 生成引擎 | FR-RP-05 | §3.2.4 审阅评分 |
| TC-ST-001 | 报告保存后文件落盘 | 存储层 | FR-RP-06 | §3.4 存储层 |
| TC-ST-002 | 报告查询分页排序 | 存储层 | FR-RP-07 | §3.4 存储层 |
| TC-ST-003 | 报告删除（DB+文件） | 存储层 | FR-RP-08 | §3.4 存储层 |
| TC-ST-004 | 模板表 CRUD | 存储层 | FR-RP-09 | §4 数据模型 |
| TC-ST-005 | 项目配置 upsert/get | 存储层 | FR-PM-01 | §4 数据模型 |
| TC-ST-006 | 发送日志记录 | 存储层 | FR-RP-10 | §4 数据模型 |
| TC-SD-001 | 发送通道选择 email_mcp | 发送层 | FR-SEND-01 | §3.3 发送层 |
| TC-SD-002 | 单收件人发送成功（mock） | 发送层 | FR-SEND-02 | §3.3 发送层 |
| TC-SD-003 | 多收件人全部投递 | 发送层 | FR-SEND-03 | §3.3 发送层 |
| TC-SD-004 | 空收件人自动跳过 | 发送层 | FR-SEND-04 | §3.3 发送层 |
| TC-API-001 | 生成日报接口 | API | 接口需求 1 | §3.5 API 层 |
| TC-API-002 | 报告列表接口 | API | 接口需求 2 | §3.5 API 层 |
| TC-API-003 | 报告详情接口 | API | 接口需求 3 | §3.5 API 层 |
| TC-API-004 | 报告不存在返回 404 | API | 接口需求 4 | §3.5 API 层 |
| TC-API-005 | 发送报告接口 | API | 接口需求 5 | §3.5 API 层 |
| TC-API-006 | 采集预览接口 | API | 接口需求 6 | §3.5 API 层 |
| TC-API-007 | 调度状态/触发接口 | API | 接口需求 7 | §3.5 API 层 |
| TC-API-008 | 项目配置接口 | API | 接口需求 8 | §3.5 API 层 |
| TC-SCH-001 | 手动触发日报调度 | 调度层 | FR-SCH-01 | §3.4 调度层 |
| TC-E2E-001 | 端到端全链路 | 端到端 | AC-05 | §2.2 总体架构 |

---

## 二、详细用例

### TC-SYS-001 系统健康检查

- **前置条件**：服务已启动，环境变量走 mock 配置。
- **步骤**：
  1. 请求 `GET /`。
- **期望效果**：
  1. 返回 HTTP 200；
  2. 响应体含服务标识字段（如 `service` / `status`），值为 `ok` 或等价健康状态。

### TC-DC-001 Jira 数据采集（mock）

- **前置条件**：`JIRA_URL` 等凭据为空（mock 数据回退），采集器可正常构建。
- **步骤**：
  1. 调用采集器执行一次采集；
  2. 检查返回的采集数据对象。
- **期望效果**：
  1. 采集不抛异常；
  2. 返回对象含 `issues_total` 字段且为整数（可为 0）；
  3. 返回对象含 `status_summary` 字典；
  4. 返回对象含 `issues` 列表。

### TC-DC-002 状态摘要统计正确性

- **前置条件**：mock 采集数据可用。
- **步骤**：
  1. 执行采集；
  2. 对 `status_summary` 各状态计数求和，与 `issues_total` 比对。
- **期望效果**：
  1. `sum(status_summary.values()) == issues_total`；
  2. `status_summary` 非空字典。

### TC-DC-003 阻塞任务识别

- **前置条件**：mock 采集数据中含阻塞任务（关键词 Blocked / 阻塞）。
- **步骤**：
  1. 执行采集；
  2. 检查采集数据中是否标记阻塞任务。
- **期望效果**：
  1. 采集结果提供阻塞标记字段（如 `blocked_total` 或 issue 级 `is_blocked`）；
  2. 阻塞任务数 ≥ 1（mock 数据保证），未阻塞任务不被误标。

### TC-DC-004 空数据采集不报错

- **前置条件**：采集源返回空数据（mock 空数据集）。
- **步骤**：
  1. 执行采集；
  2. 检查返回结果。
- **期望效果**：
  1. 不抛异常；
  2. `issues_total == 0`；
  3. `status_summary` 为空或仅含零值，且后续生成流程可正常调用。

### TC-DC-005 大盘摘要接口（mock 回退）

- **前置条件**：服务启动，大盘表未配置真实文件。
- **步骤**：
  1. 请求 `GET /api/dashboard/summary`。
- **期望效果**：
  1. 返回 HTTP 200；
  2. 响应含 `data_source` 字段，值为 `mock` 或 `cache`（mock 回退模式）。

### TC-GEN-001 日报生成（mock LLM）

- **前置条件**：`LLM_PROVIDER=mock`，采集数据可用。
- **步骤**：
  1. 调用生成引擎生成日报；
  2. 检查返回的报告字典。
- **期望效果**：
  1. 返回字典含 `title`、`content_md`、`content_html`、`source_data`；
  2. `content_md` 非空且包含「总体进度 / 风险」等关键章节标题；
  3. 不产生异常且耗时在合理范围。

### TC-GEN-002 周报生成（mock LLM）

- **前置条件**：`LLM_PROVIDER=mock`，采集数据可用。
- **步骤**：
  1. 调用生成引擎生成周报。
- **期望效果**：
  1. 返回字典结构与日报一致；
  2. `title` 含「周报」或报告类型标记；
  3. `content_md` 非空。

### TC-GEN-003 模板渲染含关键章节

- **前置条件**：模板存在或使用默认模板，采集数据可用。
- **步骤**：
  1. 用渲染函数按模板渲染采集数据。
- **期望效果**：
  1. 渲染结果含模板中定义的章节（如「项目总体进度」「今日完成」「风险与阻塞」「缺陷统计」）；
  2. 完成率等数值被正确填充。

### TC-GEN-004 HTML 邮件渲染

- **前置条件**：已生成 Markdown 报告内容。
- **步骤**：
  1. 调用 HTML 渲染函数生成完整 HTML；
  2. 检查 HTML 结构。
- **期望效果**：
  1. 输出为 HTML 字符串且含 `<html>` 或邮件容器标签；
  2. HTML 内包含关键统计信息（如完成率、任务数）。

### TC-GEN-005 审阅评分落库

- **前置条件**：生成流程完整执行，存储可用。
- **步骤**：
  1. 生成报告；
  2. 查询存储中的报告记录。
- **期望效果**：
  1. 报告记录含审阅字段（如 `review_score`）；
  2. `review_score` 为数值（mock 模式为合理常量），且在 0–100 区间。

### TC-ST-001 报告保存后文件落盘

- **前置条件**：输出目录可写，生成流程可用。
- **步骤**：
  1. 保存一份报告（md + html 双文件）；
  2. 检查输出目录。
- **期望效果**：
  1. 返回记录含报告 ID；
  2. 输出目录存在对应 `.md` 与 `.html` 文件；
  3. 文件内容与报告 `content_md` / `content_html` 一致。

### TC-ST-002 报告查询分页排序

- **前置条件**：已保存多条报告。
- **步骤**：
  1. 按分页参数查询报告列表；
  2. 检查排序。
- **期望效果**：
  1. 返回列表长度 ≤ 每页条数；
  2. 默认按创建时间倒序，最新记录在首位；
  3. 分页参数合法时返回 `total` 总数。

### TC-ST-003 报告删除（DB+文件）

- **前置条件**：存在一条已保存报告。
- **步骤**：
  1. 删除该报告；
  2. 再次查询该报告。
- **期望效果**：
  1. 删除操作返回成功；
  2. 再次查询返回空 / 不存在；
  3. 对应输出文件已被清理（或按配置归档）。

### TC-ST-004 模板表 CRUD

- **前置条件**：存储可用。
- **步骤**：
  1. 保存一个新模板；
  2. 按名称查询模板列表；
  3. 修改模板内容后再次保存。
- **期望效果**：
  1. 模板保存成功；
  2. 模板列表包含新模板，字段完整；
  3. 重复保存可覆盖更新，不产生重复记录。

### TC-ST-005 项目配置 upsert/get

- **前置条件**：存储可用。
- **步骤**：
  1. 写入项目配置（项目名、收件人列表）；
  2. 按项目 key 读取配置；
  3. 再次写入不同收件人后重读。
- **期望效果**：
  1. 首次写入返回成功；
  2. 读取结果与写入一致；
  3. 二次写入为覆盖更新，重读为最新值。

### TC-ST-006 发送日志记录

- **前置条件**：至少执行过一次发送（成功或跳过均可）。
- **步骤**：
  1. 查询发送日志列表。
- **期望效果**：
  1. 日志列表非空；
  2. 每条日志含报告 ID、收件人、状态字段（`success` / `failed` / `skipped`）。

### TC-SD-001 发送通道选择 email_mcp

- **前置条件**：`SENDER_CHANNEL=email_mcp`（或默认通道即 email_mcp）。
- **步骤**：
  1. 通过发送工厂获取发送器。
- **期望效果**：
  1. 返回的发送器类型为 Email MCP 适配器（类名含 `EmailMcp` / `EmailMCP` 或通道映射命中）；
  2. 不因通道缺失抛出异常。

### TC-SD-002 单收件人发送成功（mock）

- **前置条件**：发送器可用，收件人配置为单人。
- **步骤**：
  1. 构造最小报告内容，调用发送器发送；
  2. 检查返回结果。
- **期望效果**：
  1. 返回成功状态；
  2. 发送日志记录 `success`，收件人为单人邮箱。

### TC-SD-003 多收件人全部投递

- **前置条件**：项目配置含多个收件人。
- **步骤**：
  1. 使用多收件人配置发送报告。
- **期望效果**：
  1. 返回成功；
  2. 日志收件人字段包含全部收件人（逗号分隔或列表）。

### TC-SD-004 空收件人自动跳过

- **前置条件**：项目配置收件人为空。
- **步骤**：
  1. 尝试发送报告。
- **期望效果**：
  1. 不抛异常；
  2. 返回跳过（`skipped`）状态；
  3. 日志记录 `skipped` 且原因标记为无收件人。

### TC-API-001 生成日报接口

- **前置条件**：服务启动，mock 环境。
- **步骤**：
  1. `POST /api/reports/generate`，body 指定 `report_type=daily`。
- **期望效果**：
  1. 返回 HTTP 200；
  2. 响应含 `report_id`；
  3. 数据库新增该报告。

### TC-API-002 报告列表接口

- **前置条件**：已存在 ≥1 条报告。
- **步骤**：
  1. `GET /api/reports?page=1&page_size=10`。
- **期望效果**：
  1. 返回 HTTP 200；
  2. 响应含 `items` 数组与 `total` 总数；
  3. 最新报告位于首位。

### TC-API-003 报告详情接口

- **前置条件**：存在一条已知报告。
- **步骤**：
  1. `GET /api/reports/{report_id}`。
- **期望效果**：
  1. 返回 HTTP 200；
  2. 响应含 `id`、`report_type`、`content_md` 等核心字段。

### TC-API-004 报告不存在返回 404

- **前置条件**：服务启动。
- **步骤**：
  1. `GET /api/reports/not-exist-id`。
- **期望效果**：
  1. 返回 HTTP 404；
  2. 响应含错误信息。

### TC-API-005 发送报告接口

- **前置条件**：存在一条报告，发送层 mock 可用。
- **步骤**：
  1. `POST /api/reports/{report_id}/send`。
- **期望效果**：
  1. 返回 HTTP 200；
  2. 响应含发送结果（`success` / `skipped`）与报告 ID；
  3. 生成对应发送日志。

### TC-API-006 采集预览接口

- **前置条件**：服务启动，mock 采集可用。
- **步骤**：
  1. `GET /api/collect/preview`。
- **期望效果**：
  1. 返回 HTTP 200；
  2. 响应含采集摘要（如 `issues_total`、`status_summary`）。

### TC-API-007 调度状态/触发接口

- **前置条件**：调度器已初始化。
- **步骤**：
  1. `GET /api/scheduler/status`；
  2. `POST /api/scheduler/trigger/daily`（或等价触发接口）。
- **期望效果**：
  1. 状态接口返回 HTTP 200，含调度任务信息；
  2. 触发接口返回 HTTP 200 且调度执行成功。

### TC-API-008 项目配置接口

- **前置条件**：服务启动。
- **步骤**：
  1. `GET /api/projects/{project_key}`；
  2. `PUT /api/projects/{project_key}` 写入配置。
- **期望效果**：
  1. 未配置时 GET 返回空配置或 404（按实现）；
  2. PUT 后 GET 返回一致配置；
  3. 收件人字段正确回读。

### TC-SCH-001 手动触发日报调度

- **前置条件**：调度器可用，mock 环境。
- **步骤**：
  1. 调用调度器执行日报任务（`run_daily`）。
- **期望效果**：
  1. 任务执行成功；
  2. 生成一份日报记录并保存到存储。

### TC-E2E-001 端到端全链路

- **前置条件**：全部模块可用，mock 环境。
- **步骤**：
  1. 执行采集 → 生成 → 存储 → 发送 全流程。
- **期望效果**：
  1. 采集返回数据；
  2. 生成返回报告；
  3. 报告成功保存并落盘；
  4. 发送返回成功或按配置跳过，且发送日志落库；
  5. 全程无未捕获异常。

---

## 三、用例与验收脚本映射

验收脚本 `tests/test_acceptance_report.py` 中每个测试函数以 `test_TC_<编号>` 命名，与上表一一对应：

| 用例编号 | 脚本函数 |
|----------|----------|
| TC-SYS-001 | `test_TC_SYS_001` |
| TC-DC-001 | `test_TC_DC_001` |
| TC-DC-002 | `test_TC_DC_002` |
| TC-DC-003 | `test_TC_DC_003` |
| TC-DC-004 | `test_TC_DC_004` |
| TC-DC-005 | `test_TC_DC_005` |
| TC-GEN-001 | `test_TC_GEN_001` |
| TC-GEN-002 | `test_TC_GEN_002` |
| TC-GEN-003 | `test_TC_GEN_003` |
| TC-GEN-004 | `test_TC_GEN_004` |
| TC-GEN-005 | `test_TC_GEN_005` |
| TC-ST-001 | `test_TC_ST_001` |
| TC-ST-002 | `test_TC_ST_002` |
| TC-ST-003 | `test_TC_ST_003` |
| TC-ST-004 | `test_TC_ST_004` |
| TC-ST-005 | `test_TC_ST_005` |
| TC-ST-006 | `test_TC_ST_006` |
| TC-SD-001 | `test_TC_SD_001` |
| TC-SD-002 | `test_TC_SD_002` |
| TC-SD-003 | `test_TC_SD_003` |
| TC-SD-004 | `test_TC_SD_004` |
| TC-API-001 | `test_TC_API_001` |
| TC-API-002 | `test_TC_API_002` |
| TC-API-003 | `test_TC_API_003` |
| TC-API-004 | `test_TC_API_004` |
| TC-API-005 | `test_TC_API_005` |
| TC-API-006 | `test_TC_API_006` |
| TC-API-007 | `test_TC_API_007` |
| TC-API-008 | `test_TC_API_008` |
| TC-SCH-001 | `test_TC_SCH_001` |
| TC-E2E-001 | `test_TC_E2E_001` |

> 注：用例预期基于「mock 环境」设计（`LLM_PROVIDER=mock`、`SENDER_CHANNEL=email_mcp` 且网络不可用时自动回退），确保脚本可离线稳定运行。
